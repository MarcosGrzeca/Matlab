algoritmo 'compiladores'

var
	x, minVal, b, fb, fx, a, B, fa : real

inicio
	x <- 10;
	minVal <- 14;
	SE (x >= minVal) ENTAO
		b <- x;
		fb <- fx;
	SENAO SE (a < B) ENTAO
		a <- x;
		fa <- fx;
	FIMSE
	a <- x;
	fa <- fx;

fimalgoritmo