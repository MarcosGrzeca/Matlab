algoritmo 'compiladores'

var
	a, b, x, minVal, c, d, e, f, y : real

inicio
	a <- (b)
	x <- 10;
	minVal <- 14;
	a <- (b + c - d * e / f^2)
	SE (x >= minVal) ENTAO
		a <- x;
	SENAO
		SE (x = minVal) ENTAO
			b <- x;
		SENAO
			SE (x <= minVal) ENTAO
				c <- x;
			SENAO
				y <- x;
			FIMSE
		FIMSE
	FIMSE


fimalgoritmo