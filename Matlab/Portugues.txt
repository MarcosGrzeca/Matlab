algoritmo 'compiladores'

var
	x, minVal, a, b, c, y : real

inicio
	x <- 10;
	minVal <- 14;
	SE (x >= minVal) ENTAO
		a <- x;
	SENAO
		SE (x = minVal) ENTAO
			b <- x;
		SENAO
			SE (x <= minVal) ENTAO
				c <- x;
			SENAO
				y <- x;
			FIMSE
		FIMSE
	FIMSE

fimalgoritmo